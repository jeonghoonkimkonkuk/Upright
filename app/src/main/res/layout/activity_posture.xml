<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".ui.tutorial.PostureActivity"
    android:gravity="center">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@drawable/standing"
        android:backgroundTint="@color/purple_light_background"
        android:gravity="center">
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="바른 자세 측정하기"
            android:id="@+id/setPostureBtn"
            android:background="@drawable/gradient_background_radius20"
            android:padding="10dp"
            android:textColor="@color/white"
            android:layout_marginBottom="20dp"/>
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="메인으로 돌아가기"
            android:id="@+id/mainBtn"
            android:padding="10dp"
            android:textColor="@color/colorPrimary"/>
    </LinearLayout>

</LinearLayout>